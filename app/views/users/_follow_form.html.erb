<% unless current_user?(@user) %> <!--ログインしているユーザー自身以外の時は（自分以外の時のみ）-->
<div id="follow_form">
  <% if current_user.following?(@user) %>
  <%= render "unfollow" %>
  <% else %>
  <%= render "follow" %>
  <% end %>
</div>
<% end %>